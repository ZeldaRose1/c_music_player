// #include "Config.h"
#include "database/Database.h"
#include "view/View.h"
#include "taglib/tag_c.h"
#include <ncurses.h>

using namespace std;

int main(int argc, char ** argv) {

    Database d;
    // MainView v;
    // d.scanMusicFolder();
    // d.createTables();
    // d.addTrack("/data/Music/divine_lollia.mp3");
    // d.scanMusicFolder();

    // int x, y;
    // x = y = 10;

    // initscr(); // Starts curses mode // Initializes screen
    // move(y, x); // Moves cursor to desired y, x location // y is number of rows and x is number of columns
    // printw("Hello world"); // prints string(const char *) to a window
    // refresh(); // refresh the output
    // int c = getch();
    // mvprintw(0, 0, "%d", c); // Move and then print

    // refresh(); // unneeded here

    // clear(); // Clears standard screen

    // getch(); // wait for user input, returns int value of that key
    // endwin(); // close window // Deallocates memory and ends ncurses


    return 0;
};